sudo: required

language: generic

services:
 - docker

script:
 - docker build -t cryptofacilities/parity:1.11.11 .
 - echo $DOCKER_PASSWORD | docker login -u $DOCKER_USER --password-stdin
 - docker push cryptofacilities/parity:1.11.11
